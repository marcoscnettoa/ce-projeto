# must be unique in a given SonarQube instance

sonar.sourceEncoding=UTF-8

sonar.exclusions=public/**,vendor/**,storage/**,node_modules/**,config/**,resources/lang/**
sonar.exclusions+=, **/*.blade.php, **/*.xml, **/*.yaml, **/*.yml

sonar.coverage.exclusions=/**

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
# This property is optional if sonar.modules is set.
sonar.sources=app

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8

sonar.projectName=Erp lxxxtxx Viagens e Turismo

sonar.projectKey=32673_erp_lxxxtxx_viagens_e_turismo

sonar.projectVersion=1.0.0

sonar.language=php
